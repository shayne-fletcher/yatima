# The Yatima Maintainer Walkthrough

This mdBook rebuilds the working knowledge needed to understand, change, and debug Yatima after time away from the codebase.

Build it from this directory with:

```bash
mdbook build
```

For a local server that rebuilds as chapters change, run:

```bash
mdbook serve
```
